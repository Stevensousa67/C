#include <stdio.h>

/** This homework covers the topics of Loops, Logic, Functions, Libraries, and Unit Tests.
 * Create a program similar to Example 3 from Day 5. It should include a `main.c' `myMath.c' and `myMath.h'
 * Choose 4 of the following. All 4 functions should be declared in `myMath.h' and defined in `myMath.c'
 *
 */

int main(){
    char caseChoice;

    printf("Enter a number from 1 to 5");
    scanf("%c", &caseChoice);

    switch (caseChoice){

        case '1':{

            /** 1) Create a function called isTriangleShape. It should take in 3 ints and test that the sum of the two
             * smaller numbers is larger than the 3rd. If the test passes, the result should be true, and if it the
             * result should be false. Create the following UnitTest cases for this function:
             *
             * a = 5, b = 2, c = 4 should result in True
             * a = 1, b = 2, c = 3 should result in False
             *
             */


        }// end case 1
    }// end switch
}// end main